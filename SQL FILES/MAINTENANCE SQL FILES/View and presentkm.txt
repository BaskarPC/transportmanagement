CREATE VIEW PRESENTKM AS select JOB_ID,PRESENTKM from FUELENTRY_INSIDE UNION select JOB_ID,PRES
ENTKM from FUELENTRY_OUTSIDE;

SELECT JOB_ID FROM PRESENTKM where JOB_ID in(select MAX(JOB_ID) from MAINTENANCE_MASTER WHERE VEHICLE_NUMBER =15 AND GOODS_TYPE=132